function changeText() {
  // Reference an HTML element
  const heading = document.getElementById("message");
  
  // Change the text content
  heading.textContent = "The script with 'defer' worked successfully!";
}
